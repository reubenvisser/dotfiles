general {
    colors = true
    interval = 1
}

order += "volume master"
order += "tztime local"

volume master {
    format = "VOL %volume"
    format_muted = "VOL muted"
    device = "pulse"
}

tztime local {
    format = "%a %d %b %I:%M:%S %p"
}
